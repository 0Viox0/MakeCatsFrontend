const LineUnderChoices = ({ marginTop }: { marginTop: string }) => {
    return (
        <div
            className="w-full h-[1px] bg-[#aeaeae]"
            style={{
                marginTop,
            }}
        ></div>
    );
};

export default LineUnderChoices;
