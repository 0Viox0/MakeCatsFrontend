const Whiteline = ({ width }: { width: string }) => {
    return (
        <div
            className="h-[15px] bg-[#d9d9d9]"
            style={{
                width,
            }}
        ></div>
    );
};

export default Whiteline;
